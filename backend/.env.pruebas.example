# Copiar a `backend/.env.pruebas.local` (no se commitea). Base de los tests de integración.
DATABASE_URL=postgresql://nexo_app:cambiar-app@localhost:5433/nexo_pruebas
DATABASE_URL_MIGRACIONES=postgresql://nexo_migrador:cambiar-migrador@localhost:5433/nexo_pruebas
