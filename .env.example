# Copiar a `.env` (no se commitea) para `compose.yaml`.
# Valores solo para desarrollo local.
POSTGRES_PORT=5433
POSTGRES_PASSWORD=cambiar-postgres
NEXO_MIGRADOR_PASSWORD=cambiar-migrador
NEXO_APP_PASSWORD=cambiar-app
